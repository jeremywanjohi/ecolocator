const map = require('../assets/travel.png')
const store = require('../assets/store.png')
const waste = require('../assets/waste.png')
const support = require('../assets/help-desk.png')

export default {
    map: map,
    store: store,
    waste: waste,
    support: support,
}